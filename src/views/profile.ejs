<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meu Perfil - EducaTech</title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>

    <div class="container-lg">
        <div class="card">
            <h2>Meu Perfil</h2>

            <div class="avatar-circle">
                <% if (avatarUrl) { %>
                    <img src="<%= avatarUrl %>" alt="Avatar">
                    <% } else { %>
                        <span>
                            <%= username.charAt(0).toUpperCase() %>
                        </span>
                        <% } %>
            </div>
            <div class="text-center mb-4">
                <a href="/perfil/novo-avatar" class="btn btn-small btn-secondary">Trocar Avatar</a>
            </div>

            <form method="POST" action="/perfil/atualizar">
                <div class="form-group">
                    <label>Nome de Usuário</label>
                    <input type="text" value="<%= username %>" readonly>
                </div>

                <div class="flex gap-4">
                    <div class="form-group w-full">
                        <label>Idade</label>
                        <input type="text" value="<%= idade %> anos" readonly>
                    </div>
                    <div class="form-group w-full">
                        <label>Turma</label>
                        <input type="text" value="<%= turma %>" readonly>
                    </div>
                </div>

                <div class="form-group">
                    <label for="perfil-descricao">Sobre Mim</label>
                    <textarea id="perfil-descricao" name="descricao" rows="4"
                        placeholder="Conte um pouco sobre você..."><%= descricao %></textarea>
                </div>

                <div class="flex gap-4 mt-4">
                    <a href="/aluno/opcoes" class="btn btn-secondary w-full text-center">⬅ Voltar ao Menu Principal</a>
                </div>

                <div class="flex gap-4 mt-4">
                    <button type="submit" class="btn btn-primary">Salvar Alterações</button>
                    <a href="/logout" class="btn btn-secondary text-center">Sair</a>
                </div>
            </form>
        </div>
    </div>

</body>

</html>