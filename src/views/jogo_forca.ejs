<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= game.title %> - Forca
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/jogo_forca.css">
</head>

<body>
    <div class="container">
        <div class="card">
            <div class="header-game">
                <a href="/jogos" class="btn-back">⬅ Voltar</a>
                <h2>
                    <%= game.title %>
                </h2>
            </div>

            <p class="text-center">
                <%= game.description %>
            </p>

            <div class="hangman-container" id="game-area">
                <div class="gallows-area" id="gallows">
                    🤔
                </div>

                <div class="hint-box mb-4">
                    <strong>Dica:</strong> <span id="hint-display">...</span>
                </div>

                <div class="word-slots" id="word-slots"></div>

                <div class="keyboard" id="keyboard"></div>

                <div class="mt-4">
                    Vidas restantes: <span id="lives" style="font-weight: bold; color: red;">6</span>
                </div>
            </div>

            <div id="game-over-screen" style="display: none; text-align: center;">
                <h2 id="game-over-title"></h2>
                <p id="game-over-message"></p>
                <p id="game-over-score" style="font-size: 1.2rem; font-weight: bold; margin: 15px 0;"></p>
                <button onclick="location.reload()" class="btn btn-primary">Jogar Novamente</button>
            </div>
        </div>
    </div>

    <script>
        // ✅ CORREÇÃO: Usamos window.words e removemos o espaço do EJS
        window.words = <%- JSON.stringify(words || []) %>;
    </script>

    <script src="/js/jogo_forca.js"></script>
</body>

</html>