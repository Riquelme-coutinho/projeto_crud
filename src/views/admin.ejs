<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Painel do Professor - EducaTech</title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <div class="card">
            <div class="text-center mb-4">
                <h2>Painel do Professor</h2>
                <p>Gerencie seus alunos e jogos aqui.</p>
            </div>

            <hr style="border-top: 1px solid var(--border-color); margin: 1.5rem 0;">

            <div class="form-group">
                <label>Gestão de Turma</label>
                <div style="display: flex; gap: 10px; flex-wrap: wrap;">
                    <a href="/admin/alunos" class="btn btn-primary" style="flex: 1;">
                        👥 Gerenciar Alunos
                    </a>
                    <a href="/cadastro" class="btn btn-secondary" style="flex: 1;">
                        ➕ Cadastrar Aluno
                    </a>
                </div>
            </div>

            <div class="form-group">
                <label>Conteúdo - Criar Jogos</label>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px;">
                    <a href="/admin/quiz/novo" class="btn btn-primary">
                        ❓ Quiz
                    </a>
                    <a href="/admin/memoria/novo" class="btn btn-primary">
                        🧠 Memória
                    </a>
                    <a href="/admin/digitacao/novo" class="btn btn-primary">
                        ⌨️ Digitação
                    </a>
                    <a href="/admin/palavras/novo" class="btn btn-primary">
                        🔠 Palavras (Scramble)
                    </a>
                    <a href="/admin/forca/novo" class="btn btn-primary">
                        😵 Forca
                    </a>
                    <a href="/admin/matematica/novo" class="btn btn-primary">
                        ➗ Matemática
                    </a>
                </div>
            </div>

            <hr style="border-top: 1px solid var(--border-color); margin: 1.5rem 0;">

            <div class="form-group">
                <label>Meus Jogos Criados</label>
                <% if (typeof games !=='undefined' && games.length> 0) { %>
                    <div style="display: flex; flex-direction: column; gap: 10px;">
                        <% games.forEach(game=> { %>
                            <div
                                style="display: flex; justify-content: space-between; align-items: center; padding: 10px; border: 1px solid #ddd; border-radius: 8px; background: #fff;">
                                <div>
                                    <strong>
                                        <%= game.title %>
                                    </strong>
                                    <span style="font-size: 0.8rem; color: #666; margin-left: 10px;">
                                        (<%= game.type.charAt(0).toUpperCase() + game.type.slice(1) %>)
                                    </span>
                                </div>
                                <a href="/admin/<%= game.type %>/<%= game.id %>/editar"
                                    class="btn btn-secondary btn-small">
                                    ✏️ Editar
                                </a>
                            </div>
                            <% }) %>
                    </div>
                    <% } else { %>
                        <p style="color: #666; font-style: italic;">Você ainda não criou nenhum jogo.</p>
                        <% } %>
            </div>

            <div class="mt-4">
                <a href="/logout" class="btn btn-secondary">
                    Sair do Sistema
                </a>
            </div>
        </div>
    </div>
</body>

</html>