<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= pageTitle %> - EducaTech
    </title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/editar_jogo_matematica.css">
</head>

<body>
    <div class="container-lg">
        <div class="card">
            <h2>
                <%= isCreate ? '➕ ' + pageTitle : '✏️ ' + pageTitle %>
            </h2>
            <form action="<%= action %>" method="POST">
                <div class="form-group">
                    <label for="title">Título do Nível</label>
                    <input type="text" id="title" name="title" value="<%= level.title %>" required>
                </div>

                <div class="form-group">
                    <label>Operações Permitidas</label>
                    <div style="display: flex; gap: 20px;">
                        <label><input type="checkbox" name="operations" value="+"
                                <%=level.config.operations.includes('+') ? 'checked' : '' %>> Soma (+)</label>
                        <label><input type="checkbox" name="operations" value="-"
                                <%=level.config.operations.includes('-') ? 'checked' : '' %>> Subtração (-)</label>
                        <label><input type="checkbox" name="operations" value="*"
                                <%=level.config.operations.includes('*') ? 'checked' : '' %>> Multiplicação (×)</label>
                        <label><input type="checkbox" name="operations" value="/"
                                <%=level.config.operations.includes('/') ? 'checked' : '' %>> Divisão (÷)</label>
                    </div>
                </div>

                <div class="flex gap-4">
                    <div class="form-group w-full">
                        <label for="min">Valor Mínimo</label>
                        <input type="number" id="min" name="min" value="<%= level.config.min %>" required>
                    </div>
                    <div class="form-group w-full">
                        <label for="max">Valor Máximo</label>
                        <input type="number" id="max" name="max" value="<%= level.config.max %>" required>
                    </div>
                </div>

                <div class="form-group">
                    <label for="questions">Número de Perguntas</label>
                    <input type="number" id="questions" name="questions" value="<%= level.config.questions %>" min="1"
                        max="50" required>
                </div>

                <div class="flex gap-4 mt-4">
                    <button type="submit" class="btn btn-primary">
                        <%= isCreate ? '✨ Criar Nível' : '💾 Salvar Alterações' %>
                    </button>
                    <a href="/admin/matematica" class="btn btn-secondary">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</body>

</html>