<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= game.title %> - Matemática</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/jogo_matematica.css">
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="header-game">
                <a href="/jogos" class="btn-back">⬅ Voltar</a>
                <h2><%= game.title %></h2>
            </div>
            
            <p class="text-center">
                <%= game.description %>
            </p>

            <div class="math-container">
                <div class="score-board">
                    <div class="score-item">Acertos: <span id="score">0</span></div>
                    <div class="score-item">Questão: <span id="q-current">1</span>/<span id="q-total">?</span></div>
                </div>

                <div class="equation-box" id="equation"></div>

                <div>
                    <input type="number" class="answer-input" id="answer-input" placeholder="?">
                    <button onclick="checkAnswer()" class="btn btn-primary ml-2">Responder</button>
                </div>

                <div class="progress-bar">
                    <div class="progress-fill" id="progress"></div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // ✅ CORREÇÃO: Usamos window.config para o JS externo ler
        window.config = <%- JSON.stringify(game.config_json || {}) %>;
    </script>
    
    <script src="/js/jogo_matematica.js"></script>
</body>
</html>