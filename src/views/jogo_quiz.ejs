<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz - EducaTech</title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/jogo_quiz.css">
</head>

<body>
    <div class="quiz-container">
        <h1 style="text-align: center; margin-bottom: 30px;">🎯 Quiz Educativo</h1>

        <% quizzes.forEach((quiz, index)=> { %>
            <div class="quiz-card quiz-step" id="step-<%= index %>" style="display: none;">
                <span class="question-badge">Questão <%= index + 1 %> de <%= quizzes.length %></span>

                <% if (quiz.url_imagem) { %>
                    <img src="<%= quiz.url_imagem %>" alt="Imagem do Quiz" class="quiz-image">
                    <% } %>

                        <h3>
                            <%= quiz.pergunta %>
                        </h3>

                        <form class="quiz-form" data-id="<%= quiz.id_quiz %>" data-index="<%= index %>">
                            <label class="quiz-option">
                                <input type="radio" name="quiz_<%= quiz.id_quiz %>" value="true">
                                <span>✅ Verdadeiro</span>
                            </label>

                            <label class="quiz-option">
                                <input type="radio" name="quiz_<%= quiz.id_quiz %>" value="false">
                                <span>❌ Falso</span>
                            </label>

                            <button type="submit" class="btn btn-primary mt-4 btn-check">Conferir Resposta</button>
                        </form>

                        <div class="hidden result-box">
                            <p class="result-text" style="margin:0; color: inherit;"></p>
                            <div style="margin-top: 15px;">
                                <button class="btn btn-secondary btn-prev" style="display: none; margin-right: 10px;">⬅️
                                    Anterior</button>
                                <button class="btn btn-secondary btn-next" style="display: none;">Próxima Pergunta
                                    ➡️</button>
                            </div>
                        </div>
            </div>
            <% }) %>

                <!-- Tela de Finalização -->
                <div id="quiz-completion" class="quiz-card" style="display: none; text-align: center;">
                    <h2>🎉 Parabéns!</h2>
                    <p>Você completou o Quiz Educativo.</p>
                    <div style="margin-top: 20px;">
                        <a href="/jogos" class="btn btn-secondary">⬅️ Voltar aos Jogos</a>
                        <button onclick="location.reload()" class="btn btn-primary">🔄 Jogar Novamente</button>
                    </div>
                </div>
    </div>

    <script src="/js/jogo_quiz.js"></script>
</body>

</html>