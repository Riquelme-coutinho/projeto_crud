<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= pageTitle %> - EducaTech
    </title>
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/editar_jogo_palavras.css">
</head>

<body>
    <div class="container-lg">
        <div class="card">
            <h2>
                <%= isCreate ? '➕ ' + pageTitle : '✏️ ' + pageTitle %>
            </h2>
            <form action="<%= action %>" method="POST">
                <div class="form-group">
                    <label for="word">Palavra</label>
                    <input type="text" id="word" name="word" value="<%= word.word %>" required
                        style="text-transform: uppercase;">
                    <small>A palavra será convertida para maiúsculas automaticamente.</small>
                </div>

                <div class="form-group">
                    <label for="hint">Dica</label>
                    <input type="text" id="hint" name="hint" value="<%= word.hint %>">
                </div>

                <div class="flex gap-4 mt-4">
                    <button type="submit" class="btn btn-primary">
                        <%= isCreate ? '✨ Criar Palavra' : '💾 Salvar Alterações' %>
                    </button>
                    <a href="/admin/palavras" class="btn btn-secondary">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</body>

</html>