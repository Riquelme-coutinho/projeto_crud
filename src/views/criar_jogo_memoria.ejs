<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Jogo da Memória - EducaTech</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
</head>

<body>
    <div class="container-lg">
        <div class="card">
            <div class="flex justify-between" style="align-items: center; margin-bottom: 20px;">
                <h2>🧠 Criar Jogo da Memória</h2>
                <a href="/admin" class="btn btn-secondary btn-small">Voltar</a>
            </div>

            <form action="/admin/memoria/novo" method="POST">
                <div class="form-group">
                    <label for="title">Título do Jogo</label>
                    <input type="text" id="title" name="title" required placeholder="Ex: Animais da Floresta">
                </div>

                <div class="form-group">
                    <label for="description">Descrição (opcional)</label>
                    <textarea id="description" name="description" rows="3" placeholder="Descreva o tema do jogo..."></textarea>
                </div>

                <hr style="border-top: 1px solid var(--border-color); margin: 20px 0;">

                <h3 style="margin-bottom: 15px;">Pares de Cartas</h3>
                <p style="color: var(--text-muted); margin-bottom: 15px;">Adicione pelo menos 2 pares. Use emojis ou texto curto para os ícones.</p>

                <div id="cards-container">
                    <!-- Initial 2 pairs -->
                    <div class="card-pair" style="display: flex; gap: 10px; margin-bottom: 10px;">
                        <input type="text" name="card_names[]" placeholder="Nome (ex: cat)" required style="flex: 1;">
                        <input type="text" name="card_icons[]" placeholder="Ícone (ex: 🐱)" required style="flex: 1;">
                        <button type="button" onclick="removePair(this)" class="btn btn-danger btn-small" style="width: auto;">❌</button>
                    </div>
                    <div class="card-pair" style="display: flex; gap: 10px; margin-bottom: 10px;">
                        <input type="text" name="card_names[]" placeholder="Nome (ex: dog)" required style="flex: 1;">
                        <input type="text" name="card_icons[]" placeholder="Ícone (ex: 🐶)" required style="flex: 1;">
                        <button type="button" onclick="removePair(this)" class="btn btn-danger btn-small" style="width: auto;">❌</button>
                    </div>
                </div>

                <button type="button" onclick="addPair()" class="btn btn-secondary" style="margin-bottom: 20px;">➕ Adicionar Par</button>

                <button type="submit" class="btn btn-primary">Criar Jogo</button>
            </form>
        </div>
    </div>

    <script>
        function addPair() {
            const container = document.getElementById('cards-container');
            const pairDiv = document.createElement('div');
            pairDiv.className = 'card-pair';
            pairDiv.style.cssText = 'display: flex; gap: 10px; margin-bottom: 10px;';
            pairDiv.innerHTML = `
                <input type="text" name="card_names[]" placeholder="Nome" required style="flex: 1;">
                <input type="text" name="card_icons[]" placeholder="Ícone" required style="flex: 1;">
                <button type="button" onclick="removePair(this)" class="btn btn-danger btn-small" style="width: auto;">❌</button>
            `;
            container.appendChild(pairDiv);
        }

        function removePair(button) {
            const container = document.getElementById('cards-container');
            if (container.children.length > 2) {
                button.parentElement.remove();
            } else {
                alert('Você precisa de pelo menos 2 pares!');
            }
        }
    </script>
</body>

</html>
