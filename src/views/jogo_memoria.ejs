<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jogo da Memória - EducaTech</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/jogo_memoria.css">
    <link rel="icon" href="/img/logo-educatech.svg" type="image/svg+xml">
</head>

<body>
    <div class="container-lg">
        <div class="card">
            <div class="game-header">
                <h2>🧠 Jogo da Memória</h2>
                <a href="/jogos" class="btn btn-secondary btn-small">Voltar</a>
            </div>

            <div class="game-stats">
                <div>Movimentos: <span id="moves">0</span></div>
                <div>Tempo: <span id="timer">00:00</span></div>
            </div>

            <div class="memory-game" id="memory-game">
                <!-- Cards will be injected here by JS -->
            </div>

            <div id="win-message" class="hidden win-message">
                <h3>Parabéns! Você venceu! 🎉</h3>
                <p id="final-score" style="font-size: 1.2rem; font-weight: bold; margin: 10px 0;"></p>
                <button onclick="restartGame()" class="btn btn-primary mt-2">Jogar Novamente</button>
            </div>
        </div>
    </div>

    <script>
        // Cards data from server (Passed to global scope for the external script)
        const cardsData = <%-JSON.stringify((typeof cards !== 'undefined' && cards) ? cards : []) %>;    </script>
    <script src="/js/jogo_memoria.js"></script>
</body>

</html>